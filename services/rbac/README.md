# RBAC Foundation

Role-based access helpers derive permission keys from the authenticated user's active memberships.

Future phases should define canonical permission keys, seed default roles, and enforce permissions at route, service, and mutation boundaries.
